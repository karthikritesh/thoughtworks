name: Test actions check
on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      
      - name: Run some bash commands
        run: |
          mkdir temp
          ls | grep temp
